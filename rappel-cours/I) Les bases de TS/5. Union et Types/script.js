"use strict";
// Unions
let code;
code = "10";
code = 10;
code = true;
// Tableau rempli de booléen ou de number
let arr;
arr = [true, false, 999];
const foo = (param) => console.log(param);
foo("hello");
foo(10);
const base = (param) => console.log(param);
base("Type personnalisé");
base(true);
base(100);
const button = {
    x: 100,
    y: 120,
    id: 99,
    visible: true
};
console.log(button);
